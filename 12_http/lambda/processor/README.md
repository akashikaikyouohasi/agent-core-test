# Processor Lambda（削除済み）

このディレクトリは以前のカスタムProcessor Lambda実装用でしたが、現在は使用されていません。

## 変更内容

プロジェクトは Amazon Bedrock AgentCore Runtime 統合のみをサポートするように更新されました。

カスタムProcessor Lambdaの機能が必要な場合は、代わりに AgentCore Agent内でカスタムロジックを実装してください。

詳細は `/agentcore_example/` ディレクトリを参照してください。
